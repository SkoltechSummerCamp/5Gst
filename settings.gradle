import java.nio.file.Paths

rootProject.name = "SpeedtestApplication"
include ':app', ':balancerApi'

project(':balancerApi').projectDir = file(Paths.get("swaggerApi", "AUTOBUILD", "javaBalancerClient"))
